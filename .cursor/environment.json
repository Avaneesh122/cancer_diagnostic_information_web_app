{
  "name": "Cancer Diagnostic Web App",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "web-server",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
      "description": "FastAPI + Uvicorn dev server for the slide viewer, served at http://localhost:8000"
    }
  ],
  "ports": [
    { "name": "web", "port": 8000 }
  ]
}
